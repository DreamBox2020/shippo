{
  "name": "shippo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean:sdk": "lerna run clean --scope @shippo/*",
    "build:sdk": "lerna run build --scope @shippo/*"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.15.0",
    "eslint-config-react-app": "^7.0.1",
    "lerna": "^5.1.6",
    "typescript": "^4.7.4"
  }
}